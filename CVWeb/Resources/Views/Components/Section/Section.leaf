<section class="section d-flex flex-column align-items-center" id="#get(sectionId)">
    <div class="container">
        <h2 class="section-heading mb-0">#get(title)</h2>
        #embed("Components/Divider/Divider")
        #get(content)
    </div>
</section>

<hr class="m-0">
