<section class="section d-flex flex-column align-items-center" id="#(sectionId)">
    <div class="container">
        <h2 class="section-heading mb-0">#(title)</h2>
        #extend("Components/Divider/Divider")
        #get(content)
    </div>
</section>

<hr class="m-0">
