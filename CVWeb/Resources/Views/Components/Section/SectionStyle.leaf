.section {
    padding-top: 6rem;
    padding-bottom: 6rem;
}

.section .section-heading {
    text-align: center
}

.section .section-item {
    padding-top: 2rem;
    padding-bottom: 2rem;
}

.section .section-item .section-date {
    text-align: right;
    color: var(--primary);
    white-space: nowrap;
}
